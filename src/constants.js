// Which edge the nav bar lives on for each page
export const PAGE_EDGE = {
  '/': 'bottom',
  // All other pages use 'left'
};

export const NAV_THICKNESS    = 56;  // px — height (bottom) or width (left)
export const NAV_EXPANDED_WIDTH = 160; // px — expanded width of left nav on hover
